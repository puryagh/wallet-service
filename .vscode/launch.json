{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch liveutil backend wallet service (multi-env)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/wallet-service/main.go",
            "env": {},
            "args": [],
            "cwd": "${workspaceFolder}",
            "envFile": [
                "${workspaceFolder}/dev.env"
            ]
        }
    ]
}